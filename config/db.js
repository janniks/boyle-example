/**
 * db.js
 * 
 * Configure the database for boyle
 * Default values are commented out below
 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * **/

// Configure Globals
module.exports = {

	/*
	 * address: The address of your database
	 * default: 'localhost'
	 * accepted: string
	 */
	// 'address': 'localhost',

	/*
	 * port: The port of your database
	 * default: ''
	 * accepted: string
	 */
	// 'port': '',

	/*
	 * username: The username of your database
	 * default: ''
	 * accepted: string
	 */
	// 'username': '',
	
	/*
	 * password: The password of your database
	 * default: ''
	 * accepted: string
	 */
	// 'password': '',
	
	/*
	 * database: The database name of your database
	 * default: ''
	 * accepted: string
	 */
	// 'database': '',
	
};
