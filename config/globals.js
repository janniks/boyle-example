/**
 * globals.js
 * 
 * Configure the globals of the boilerplate
 * Default values are commented out below
 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * **/

// Configure Globals
module.exports = {
	
	/*
	 * _: Determines whether lodash is accessible globally.
	 * default: true
	 * accepted: true/false type
	 */
	// '_': true,

	/*
	 * Models: Array of Models that are accessible globally with Models.<Name>
	 * default: []
	 * accepted: [string] or false type
	 * example: [ 'Person', 'Pet' ]
	 */
	'Models': [
		'User',
	],
};
