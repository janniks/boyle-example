/**
 * web.js
 * 
 * Configure the settings for the web of the boilerplate
 * Default values are commented out below
 * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * **/

// Configure Globals
module.exports = {
	
	/*
	 * static: Determines which directories are accessible statically.
	 * default: 'public'
	 * accepted: string, [string]
	 */
	// 'static': 'public',

};
